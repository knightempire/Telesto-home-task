/**
 * @type {import('next').NextConfig}
 */

const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['www.telestoenergy.com', 'i.imgur.com'],
    },
};

module.exports = nextConfig;